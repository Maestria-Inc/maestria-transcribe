# Maestria Transcription Service v4.3
# Core
setuptools>=69.0.0,<82.0.0
flask==3.1.0
flask-cors==5.0.1
requests==2.32.3
gunicorn==23.0.0

# Audio
librosa==0.10.2
soundfile==0.13.1
numpy==1.26.4

# Transcription (ByteDance)
piano_transcription_inference==0.0.6
torch==2.5.1
--extra-index-url https://download.pytorch.org/whl/cpu

# MIDI processing
pretty_midi==0.2.10

# Notation (Partitura — best-effort MusicXML)
partitura>=1.5.0
pandas>=2.0.0

# Storage (Supabase)
supabase>=2.0.0
